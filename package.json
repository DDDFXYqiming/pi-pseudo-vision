{
  "name": "pi-pseudo-vision",
  "version": "0.2.0",
  "description": "Local OCR + color-statistics + pixel-scan + metadata bridge for text-only Pi Coding Agent models. No external vision API.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-extension",
    "ocr",
    "vision",
    "pseudo-vision",
    "text-only",
    "multimodal"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "sharp": "0.35.4",
    "tesseract.js": "5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  }
}